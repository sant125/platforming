apiVersion: v1
kind: Service
metadata:
  labels:
    app: eipiai
  name: eipiai-svc
  namespace: image-registry-test 
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: eipiai
status:
  loadBalancer: {}
